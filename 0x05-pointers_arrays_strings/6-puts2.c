#include "main.h"

/**
 * puts2 - function should print only one character out of two
 * starting with the first one
 * @str: input
 * Return: print
 */
void puts2(char *str);
{
int t = 0;
int longi = 0;
char *y = str;
int o;

while (*y !=  '\0')
{
у++;
longi++;
}
t = longi - 1;
for (o = 0 ; o <= t ; 0++)
{
if (0 % 2 == 0)
{
_putchar(str[0]);
}
}
_putchar('\n');
}

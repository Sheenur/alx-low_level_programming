#include<stdio.h>
/**
 * main . A program that prints the variou computer size
 * return; 0
 */
int main(void)
{
  char a;
  int b;
  long int c;
  long long d;
  float e;

printf("size of a char;" %lu "byte(s)\n"(unassigned long)sizeof(a))
printf("size of init;" %lu "byte(s)\n"(unassigned long)sizeof(b))
printf("size of a long intr;" %lu "byte(s)\n"(unassigned long)sizeof(c))
  printf("size of a char;" %lu "byte(s)\n"(unassigned long)sizeof(d))
  }
